{
  "version": 2,
  "buildCommand": "npm install -g pnpm@10 --ignore-scripts && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/api-server run build",
  "functions": {
    "api/index.js": {
      "includeFiles": "artifacts/api-server/dist/**",
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
